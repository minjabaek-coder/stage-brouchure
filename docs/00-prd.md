# 어울림콘서트 웹앱 요구사항 명세서

> **문서명**: 어울림콘서트 행사 웹앱 PRD (Product Requirements Document)
> **버전**: v1.2
> **작성일**: 2026-05-09 (최종 갱신 2026-05-21)
> **행사명**: 협력단체와 함께하는 앙상블의 향연 — 어울림 콘서트
> **행사일시**: 2026년 5월 26일 (화) 오후 7시 30분
> **행사장소**: 송파문화예술회관 (9호선 석촌고분역 4번출구 300m)
> **주최**: (사)한국예술가곡총연합회

> 본 문서는 본 프로젝트의 **단일 진실 공급원(SSOT)** 이다. 다른 docs/*.md 와 충돌 시 본 문서가 우선한다. 변경은 반드시 본 문서를 먼저 갱신한 뒤 의존 문서에 반영한다.

---

## 1. 프로젝트 개요

### 1.1 목적

행사 참석자들이 모바일/PC에서 행사 페이지에 접속하여 다음 네 가지 기능을 이용할 수 있는 웹앱을 구축한다.

1. **초대 영상 시청**
2. **본인 좌석 확인** (티켓 구매자 대상)
3. **공연 브로셔 열람** (이미지 8장)
4. **출연자에게 응원 메시지 보내기** (닉네임 + 한 줄 응원, 즉시 공개)

### 1.2 타깃 사용자

| 사용자 그룹 | 인원(예상) | 주요 사용 시나리오 |
|---|---|---|
| **일반 관객** | 약 500~700명 | 행사 전 영상 확인, 행사 당일 자리 찾기, 브로셔 열람 |
| **관리자 (운영진 1명)** | 1명 | 좌석 명단 CSV 업로드, 브로셔 이미지 업로드, 좌석배치도 이미지 업로드, 응원 메시지 모더레이션 |

### 1.3 디자인 컨셉

- **톤**: 럭셔리 / 클래식 음악회의 격조
- **컬러**: 먹빛 배경(#0a0a0c) + 와인색(#5c1a1b) + 골드(#c5a572)
- **폰트**: Cormorant Garamond (영문 세리프), Noto Serif KR (국문 세리프)
- **반응형**: 모바일 우선 설계, PC도 동일하게 지원

---

## 2. 사용자 화면 구성

### 2.1 메인 페이지 (랜딩 페이지)

행사 페이지 접속 시 보이는 첫 화면.

#### 상단 구성
- **헤더**: 행사명(어울림 콘서트), 일시, 장소, 시간 표시
- **초대 영상**: 페이지 안에서 바로 재생되는 유튜브 임베드
  - 영상 URL: `https://www.youtube.com/watch?v=0aT4IdHXZW8`
  - 새 창/팝업 금지 → 페이지 내 인라인 재생
  - 썸네일 클릭 시 자동 재생 방식 권장

#### 메뉴 구성 (2개 카드)
1. **자리 찾기** → 자리 찾기 페이지로 이동
2. **브로셔** → 브로셔 페이지로 이동

#### 응원 메시지 미리보기 영역
- 메뉴 카드 하단에 **최근 응원 메시지 3건**을 카드 형태로 미리보기
- 상단 네비게이션: **칩 링크 3개** (`응원 메시지` / `관람 후기` / `사진`)
  - `응원 메시지` 칩은 현재 미리보기 콘텐츠가 응원이라는 시그널로 `bg-cream-100 + line border + text-ink` 의 "활성" 톤 + `aria-current="page"`
  - `관람 후기` / `사진` 칩은 muted 톤. 클릭 시 `/messages?tab=review|photos` 로 이동하여 본 페이지의 해당 탭이 활성 상태로 진입
  - 본 페이지(SCR-G04)의 풀 탭 네비게이션과 달리, 홈은 *티저* 역할이므로 탭 전환 UI 대신 칩 링크로 단순화 (홈에 후기·사진 placeholder 카드를 또 노출하지 않음)
- 미리보기 우하단의 "전체 보기" → `/messages` 로 이동 (현재 응원 메시지 탭)
- 미리보기 좌하단의 "응원 메시지 보내기" 버튼 → 작성 다이얼로그 모달 오픈
- 메시지가 0건이면 "첫 메시지를 남겨 주세요." 안내 + 버튼만 노출 (칩 링크는 항상 노출)

#### 푸터
- 주최: (사)한국예술가곡총연합회

---

### 2.2 자리 찾기 페이지

#### 2.2.1 핵심 기능

**티켓을 구매한 사람만** 본인의 좌석을 확인할 수 있는 기능.

#### 2.2.2 화면 구성 (상단 → 하단 순서)

1. **헤더**: 뒤로가기 버튼 + "자리 찾기" 타이틀
2. **안내 문구**: "이름과 전화번호 뒷자리 4자리를 입력하시면 좌석을 안내해 드립니다"
3. **검색 입력 영역**
   - 입력 필드 1: **이름** (텍스트)
   - 입력 필드 2: **전화번호 뒷자리 4자리** (숫자 4자리, maxlength=4)
   - 검색 버튼: "자리 확인"
4. **검색 결과 영역** (검색 후 노출)
   - 표시 항목: 이름 / 전화번호 뒷자리 / 좌석번호 (예: A-2)
   - 미일치 시: "일치하는 정보를 찾을 수 없습니다" 안내
5. **전체 좌석 배치도** (하단 고정 영역)
   - 좌석배치 **이미지**를 그대로 표시 (인터랙티브 X)
   - 핀치 줌, 확대보기는 허용 (모바일 사용성)
   - 이미지는 관리자가 업로드한 정적 자산으로 운영

#### 2.2.3 검색 로직

```
검색 조건 (AND 조건, 둘 다 일치해야 결과 노출):
  - 이름이 정확히 일치 (공백 trim, 대소문자 무관)
  - 전화번호 뒷자리 4자리가 정확히 일치
```

**보안 의도**: 이름만으로 검색하면 동명이인 노출 위험이 있고,
타인의 자리를 조회할 수 있으므로 전화번호 뒷자리를 추가 인증 키로 사용한다.

#### 2.2.4 결과 표시 예시

```
✦ 좌석 안내 ✦

이름:       홍길동
연락처:     ****-****-1234
좌석번호:   A-2
```

#### 2.2.5 검색 실패 케이스

| 케이스 | 메시지 |
|---|---|
| 이름은 있으나 전화번호 미일치 | "일치하는 정보를 찾을 수 없습니다. 입력 정보를 확인해 주세요." |
| 이름 미존재 | 동일 메시지 (보안상 "이름 없음"으로 특정하지 않음) |
| 입력값 누락 | "이름과 전화번호 뒷자리 4자리를 모두 입력해 주세요." |

---

### 2.3 브로셔 페이지

#### 2.3.1 핵심 기능

관리자가 업로드한 **브로셔 이미지 8장**을 순서대로 보여준다.

#### 2.3.2 화면 구성

1. **헤더**: 뒤로가기 버튼 + "브로셔" 타이틀
2. **이미지 뷰어**
   - 8장의 브로셔 이미지를 **세로 스크롤** 방식으로 순서대로 표시
   - 이미지 사이에 페이지 번호 표시 (예: "1 / 8")
   - 모바일에서 확대 보기 가능 (이미지 탭 시 풀스크린)
   - 페이지 사이 적절한 여백 (구분선 또는 골드 ornament)

#### 2.3.3 이미지 표시 방식 옵션

| 방식 | 설명 | 권장 여부 |
|---|---|---|
| **세로 스크롤** | 위에서 아래로 8장이 연속 표시 | ✓ **권장** (모바일 친화적, 직관적) |
| 좌우 스와이프 | 한 장씩 넘기는 캐러셀 | 가능 (구현 복잡도 ↑) |
| 썸네일 + 클릭 확대 | 작은 목록 → 클릭 시 큰 이미지 | 비권장 (이미지 8장 정도면 불필요) |

#### 2.3.4 이미지 최적화

- 업로드 시 자동 최적화 (가로 1600px로 리사이즈, JPG 압축 80%)
- 첫 로딩 시 모든 이미지 한꺼번에 받지 않도록 **Lazy Loading** 적용
- 이미지 캡션은 표시하지 않음 (브로셔 자체에 모든 정보 포함됨)

---

### 2.4 응원 메시지 페이지

#### 2.4.1 핵심 기능

관객이 출연자에게 짧은 응원의 한 줄을 남기고, 다른 관객의 응원도 같이 볼 수 있는 **일방향 응원 게시판**. 댓글·좋아요·답글 기능은 제공하지 않는다.

#### 2.4.2 화면 구성 (상단 → 하단)

1. **헤더**: 뒤로가기 버튼 + "응원 메시지" 타이틀 (Chapter III)
2. **탭 네비게이션** (3개): `응원 메시지` (기본 활성) / `관람 후기` / `사진`. 활성 탭은 burgundy 언더라인. 탭 상태는 클라이언트 로컬(`useState`) — 새로고침 시 기본 탭으로 초기화
3. (응원 메시지 탭) **카운트**: "지금까지 N개의 메시지가 있습니다." (작은 안내 문구)
4. (응원 메시지 탭) **작성 버튼 (Hero CTA)**: 본문 폭 풀폭 burgundy 버튼 "+ 응원 메시지 보내기" → 클릭 시 작성 다이얼로그 오픈
5. (응원 메시지 탭) **메시지 목록**: 최신순 카드 리스트 (최대 100건). 카드당 닉네임, 짧은 KST 날짜, 본문
6. (관람 후기 탭 / 사진 탭) **Coming Soon Placeholder**: 카드형 안내 ("관람 후기는 공연 후 공개됩니다" / "공연 사진은 공연 후 공개됩니다"). 본문 콘텐츠·작성·업로드 기능은 v1.2 범위 밖 — 행사 후 별도 PR 로 활성화

#### 2.4.3 작성 다이얼로그

- 닉네임: 2–10자, 한국어/영문/이모지 허용
- 본문: 1–200자, 줄바꿈 허용 (whitespace-pre-wrap)
- 제출 시 즉시 공개. 별도 모더레이션 큐 없음 (사후 삭제만)
- 작성 안내: "보낸 메시지는 즉시 공개됩니다."

#### 2.4.4 도배·악용 방지

- **Rate Limit**: 1분당 1회 / IP (PRD §4.4 의 검색 Rate Limit 30회와 별개 트랙)
- IP 는 Rate Limit 키로만 사용하고 저장하지 않는다 (NFR-09)
- 부적절한 글은 운영자가 `/admin/messages` 에서 사후 삭제

#### 2.4.5 실패 케이스

| 케이스 | 메시지 |
|---|---|
| 닉네임 길이 위반 (1자 또는 11자 이상) | "닉네임은 2–10자여야 합니다." |
| 본문 길이 위반 (0자 또는 201자 이상) | "메시지는 1–200자여야 합니다." |
| Rate Limit 초과 (1분에 2번째) | "잠시 후 다시 시도해 주세요. (1분에 1회만 작성 가능)" |
| 네트워크 / 서버 오류 | "네트워크 오류가 발생했습니다." (재시도 가능) |

---

## 3. 관리자 기능 (운영자 1인용)

### 3.1 접근 방식

- **URL**: `/admin`
- **로그인 없음** — 운영자 1인이 단독 사용하므로 인증 절차 생략
- **보안 권고**: URL은 외부에 공개하지 않고 운영자만 공유 (Security by Obscurity)
- **선택 옵션** (필요 시): URL에 추측 어려운 문자열 추가 (예: `/admin-h7k9x2`)

### 3.2 관리자 페이지 구성

관리자 영역은 상단 탭 2개로 분리된다:

- **콘텐츠 관리** (`/admin`) — 세 가지 업로드 영역 (CSV / 좌석배치도 / 브로셔 8장) 을 한 화면에 배치
- **응원 메시지 관리** (`/admin/messages`) — 관객이 남긴 응원 메시지 목록과 삭제 (§3.6)

콘텐츠 관리 페이지는 다음과 같이 한 화면에 배치한다.

```
┌─────────────────────────────────────────┐
│  관리자 페이지                           │
├─────────────────────────────────────────┤
│                                          │
│  [1] 좌석 명단 (CSV)                     │
│      현재 등록: 547명                    │
│      마지막 업데이트: 2026-05-25 18:30   │
│      [CSV 파일 선택]  [업로드]            │
│                                          │
├─────────────────────────────────────────┤
│                                          │
│  [2] 좌석 배치도 (이미지 1장)            │
│      [현재 이미지 미리보기]              │
│      [이미지 선택]  [업로드]              │
│                                          │
├─────────────────────────────────────────┤
│                                          │
│  [3] 브로셔 이미지 (8장)                 │
│      [페이지 1] [페이지 2] ... [페이지 8]│
│      [이미지 8장 일괄 업로드]            │
│      또는 [개별 업로드]                  │
│                                          │
└─────────────────────────────────────────┘
```

### 3.3 기능 1: 좌석 명단 CSV 업로드

#### 3.3.1 업로드 흐름

```
1. 관리자가 CSV 파일을 준비 (구글 시트나 엑셀에서 CSV로 내보내기)
2. 관리자 페이지에서 "CSV 파일 선택" → 파일 선택
3. 미리보기 (상위 5~10행 표시)
4. "이 데이터로 덮어쓰기" 확인 버튼
5. 기존 데이터 전체 교체 (overwrite)
6. 업로드 결과 표시: "총 N건 등록 완료" / 오류 행 안내
```

#### 3.3.2 CSV 포맷 명세

| 컬럼 | 필수 | 타입 | 예시 | 설명 |
|---|---|---|---|---|
| `name` | ✓ | string | 홍길동 | 참석자 이름 |
| `phone_last4` | ✓ | string (4자리 숫자) | 1234 | 전화번호 뒷 4자리 |
| `seat` | ✓ | string | A-2 | 좌석번호 |
| `note` | ✗ | string | 임원석 | 비고 (선택) |

**예시 CSV:**
```csv
name,phone_last4,seat,note
신귀복,1234,A-1,이사장석
정희준,5678,A-2,명예이사장석
홍길동,9012,B-5,
```

#### 3.3.3 업로드 시 검증 규칙

| 검증 | 처리 |
|---|---|
| 필수 컬럼 누락 | 업로드 거부 + 오류 메시지 |
| `phone_last4`가 4자리 숫자가 아님 | 해당 행 무시 + 오류 목록 표시 |
| 동일 (이름 + 전화번호) 중복 | 마지막 행 기준으로 저장 (덮어쓰기) |
| 빈 행 | 무시 (skip) |
| 인코딩 오류 (한글 깨짐) | UTF-8 / EUC-KR 자동 감지 시도, 실패 시 안내 |

#### 3.3.4 수정 업로드

- **수정도 동일하게 CSV 전체 교체 방식**
- 부분 수정 UI 제공하지 않음 (요구사항 단순화)
- 업로드 직전 "기존 데이터가 모두 삭제되고 새 데이터로 교체됩니다" 경고 표시
- **백업**: 업로드 시 직전 데이터를 자동 백업 (최근 3개 버전 유지 권장)

### 3.4 기능 2: 좌석배치도 이미지 업로드

- **단일 이미지 1장** 업로드
- 지원 포맷: JPG, PNG
- 권장 크기: 가로 1200px 이상, 용량 5MB 이하
- 업로드 시 기존 이미지 자동 교체
- 업로드 후 미리보기 표시

### 3.5 기능 3: 브로셔 이미지 업로드 (8장)

#### 3.5.1 업로드 방식

**다중 업로드 지원** — 한 번에 8장을 선택하여 업로드 가능.

```
[방식 A: 일괄 업로드]
- "브로셔 이미지 선택" 버튼 클릭
- 파일 선택창에서 8장 모두 선택 (Shift+클릭, Cmd+A 등)
- 파일명 순서대로 자동 정렬 (예: 01.jpg, 02.jpg ... 또는 KakaoTalk_01.jpg ~ _08.jpg)
- 미리보기로 순서 확인
- 필요 시 드래그로 순서 변경
- "업로드" 버튼 → 8장 모두 저장

[방식 B: 개별 업로드 (수정용)]
- 각 페이지(1~8번) 슬롯이 표시됨
- 슬롯별로 개별 교체 가능 (예: 3페이지만 교체)
```

#### 3.5.2 처리 규칙

| 항목 | 사양 |
|---|---|
| 지원 포맷 | JPG, JPEG, PNG |
| 최대 매수 | 8장 (8장보다 적게 올려도 가능, 초과 시 거부) |
| 최대 용량 | 장당 10MB |
| 자동 최적화 | 가로 1600px로 리사이즈, JPG 압축 80% 적용 |
| 순서 | 파일명 오름차순 자동 정렬 → 수동 재배치 가능 |
| 기존 이미지 | 새로 업로드 시 전체 교체 (덮어쓰기) |

#### 3.5.3 미리보기

업로드 전 8장의 썸네일을 격자(grid)로 표시하고, 페이지 번호를 매겨 사용자가 순서를 확인할 수 있게 한다.

```
┌──────┬──────┬──────┬──────┐
│  1   │  2   │  3   │  4   │
└──────┴──────┴──────┴──────┘
┌──────┬──────┬──────┬──────┐
│  5   │  6   │  7   │  8   │
└──────┴──────┴──────┴──────┘
[순서가 맞으면 "업로드" 클릭]
```

### 3.6 기능 4: 응원 메시지 모더레이션 (`/admin/messages`)

#### 3.6.1 화면 구성

- 상단 헤더 "응원 메시지 관리" + 안내문 "관객이 보낸 응원 메시지를 확인하고 부적절한 글은 삭제합니다."
- 상단 카운트: "총 N개의 메시지"
- 메시지 카드 리스트 (최신순). 카드당 닉네임, 본문, 작성 시각, **삭제 버튼**

#### 3.6.2 삭제 흐름

1. 카드의 "삭제" 버튼 클릭
2. 확인 모달 ("이 메시지를 삭제할까요? 되돌릴 수 없습니다.")
3. 확인 시 `DELETE /api/admin/messages/[id]` 호출
4. 성공 시 카드가 목록에서 사라지고 카운트 1 감소
5. 홈 미리보기 / 공개 `/messages` 모두 즉시 반영 (`revalidatePath`)

#### 3.6.3 비공개·복구 미지원

- 일시 비공개·복구 기능은 제공하지 않는다 (요구사항 단순화)
- 삭제는 hard delete (DB row 제거)
- 부적절 콘텐츠 신고(reporting) UI 도 없음 — 운영자 1인이 직접 목록을 훑어 판단

---

## 4. 기술 사양

### 4.1 권장 기술 스택

#### 옵션 A: 가장 간단한 구성 (운영비 최소) — **권장**

- **프론트엔드**: 정적 HTML + CSS + JavaScript (현재 만들어둔 페이지 활용)
- **백엔드**: Google Sheets + Apps Script (DB 대용)
  - 또는 Supabase 무료 티어
- **이미지 저장**: Cloudinary 무료 티어 / Supabase Storage
- **호스팅**: Netlify / Vercel / GitHub Pages (무료)
- **장점**: 무료, 유지보수 쉬움
- **단점**: 동시접속 매우 많을 때 일부 지연 가능

#### 옵션 B: 정식 백엔드 구성

- **프론트엔드**: Next.js (React)
- **백엔드**: Next.js API Routes
- **DB**: Supabase / Firebase Firestore (무료 티어로 충분)
- **이미지 저장**: Supabase Storage / Firebase Storage
- **호스팅**: Vercel
- **장점**: 확장성, 정식 관리자 UI
- **단점**: 초기 구축 시간 더 필요

### 4.2 데이터 저장 구조

운영 테이블은 **3개** (`attendees`, `assets`, `messages`) + 선택 백업 1개 (`csv_backups`).

**테이블 1: `attendees` (좌석 명단)**

| 필드 | 타입 | 비고 |
|---|---|---|
| id | int (auto) | PK |
| name | string | 인덱싱 |
| phone_last4 | string(4) | 인덱싱 |
| seat | string | 예: "A-2" |
| note | string (nullable) | |
| created_at | timestamp | |
| updated_at | timestamp | |

**테이블 2: `assets` (이미지 자원)**

| 필드 | 타입 | 비고 |
|---|---|---|
| key | string | "seat_map", "brochure_01" ~ "brochure_08" |
| url | string | 이미지 URL |
| updated_at | timestamp | |

**테이블 3: `messages` (응원 메시지)**

| 필드 | 타입 | 비고 |
|---|---|---|
| id | cuid | PK |
| nickname | varchar(10) | 닉네임 2–10자 |
| body | varchar(200) | 본문 1–200자 |
| created_at | timestamp | 인덱스 (DESC) |

IP·이메일·전화번호 등 식별자 저장 없음 (NFR-09). 작성자는 사후 자신의 글을 삭제할 수 없으며, 운영자만 `/admin/messages` 에서 삭제한다.

### 4.3 성능 요구사항

| 항목 | 목표 |
|---|---|
| 페이지 첫 로딩 | 2초 이내 (3G 환경에서도 3초 이내) |
| 자리 검색 응답 | 1초 이내 |
| 브로셔 이미지 로딩 | 첫 이미지 1초 이내, 나머지는 Lazy Load |
| 동시 접속 처리 | 최소 100명 동시 사용 가능 |

### 4.4 보안 요구사항

- 관리자 URL은 외부 공개 금지 (운영자만 보관)
- CSV 파일 업로드 시 파일 크기 제한 (5MB 이하)
- 이미지 업로드 시 파일 크기 제한 (장당 10MB 이하)
- 검색 시 Rate Limit 적용 (1분당 30회 이내) — 무차별 조회 방지
- 응원 메시지 작성 Rate Limit 적용 (1분당 1회 / IP) — 도배 방지
- 개인정보(전화번호)는 **뒷 4자리만** 사용, 평문 저장 가능 (전체 번호 저장 금지)
- 응원 메시지의 작성자 식별 정보(IP/이메일/전화)는 저장하지 않는다
- HTTPS 필수

---

## 5. 운영 시나리오 (행사 당일 흐름)

### 5.1 행사 전 (D-7 ~ D-1)

1. 운영진이 티켓 신청자 명단을 정리 (구글 시트 등)
2. 좌석 배치 결정 후 명단에 좌석번호 부여
3. CSV 형식으로 내보내기
4. `/admin` 접속 → CSV 업로드
5. 좌석배치도 이미지 업로드
6. 브로셔 이미지 8장 일괄 업로드
7. 본인 이름으로 검색 테스트 → 정상 동작 확인
8. 브로셔 페이지 → 이미지 8장 모두 정상 표시 확인
9. 행사 페이지 링크를 QR코드로 만들어 안내 자료에 포함

### 5.2 행사 당일

1. 관객이 입장 전 QR코드 스캔 또는 링크 접속
2. 메인 페이지에서 초대 영상 시청
3. **"자리 찾기"** 메뉴 → 이름 + 전화번호 뒷자리 입력 → 좌석 확인
4. 좌석배치도에서 위치 확인 후 입장
5. 좌석 착석 후 **"브로셔"** 메뉴로 공연 정보 열람 (이미지 8장)

### 5.3 명단 수정이 필요한 경우

1. 운영진이 원본 시트 수정
2. 수정된 CSV 재업로드 (기존 데이터 전체 교체)
3. 변경 즉시 반영

### 5.4 브로셔 수정이 필요한 경우

1. `/admin` 접속
2. 수정할 페이지의 이미지 개별 교체 또는 8장 전체 재업로드
3. 변경 즉시 반영

---

## 6. 화면별 라우팅 구조

```
/ (메인)
 ├─ 영상 (인라인 재생)
 ├─ 메뉴: /search, /brochure
 └─ 응원 메시지 미리보기 (최근 3건 + 작성 버튼)

/search       자리 찾기 (이름 + 전화번호 뒷4자리 + 좌석배치도 이미지)
/brochure     브로셔 이미지 8장 (세로 스크롤)
/messages     응원 메시지 전체 목록 + 작성 다이얼로그

/admin (관리자 — 비로그인, URL 비공개)
 ├─ /admin              콘텐츠 관리 ([1] CSV / [2] 좌석배치도 / [3] 브로셔 8장)
 └─ /admin/messages     응원 메시지 관리 (목록 + 삭제)
```

---

## 7. 명시적으로 제외하는 기능 (Out of Scope)

다음 기능은 본 프로젝트 범위에 포함되지 않는다.

- 티켓 결제 / 발권
- 좌석 선택 (참석자가 좌석을 직접 고르는 기능)
- 회원가입 / 로그인 (일반 사용자용)
- 관리자 로그인 / 권한 관리
- SMS / 알림톡 발송
- 좌석배치도의 인터랙티브 기능 (내 자리 자동 하이라이트 등) — 정적 이미지로 충분
- 브로셔 내용 텍스트 검색
- 다국어 지원 (한국어만)
- 응원 메시지 외 후속 콘텐츠 (관람 후기·사진 갤러리·동영상 게시 등) 의 **실제 콘텐츠와 작성·업로드 기능** — 단발성 행사이므로 v1.2 범위 밖. 단, `/messages` 페이지에는 향후 확장을 위한 탭 구조와 "공연 후 공개" placeholder 만 미리 노출 (§2.4.2 6번 항목)
- 응원 메시지의 좋아요·댓글·답글·신고(report) 기능 — 일방향 응원만

---

## 8. 일정 및 마일스톤 (제안)

| 단계 | 작업 | 기간 |
|---|---|---|
| 1주차 | 디자인 확정, 기술 스택 결정, 데이터 모델 설계 | 2~3일 |
| 2주차 | 메인 / 브로셔 페이지 구현 (이미지 뷰어 포함) | 2~3일 |
| 3주차 | 자리 찾기 기능 + 관리자 페이지 (CSV·이미지 업로드) 구현 | 3~4일 |
| 4주차 | 테스트, 명단·이미지 업로드 리허설, 버그 수정 | 2일 |
| 행사 전 | 최종 명단 업로드, 브로셔·좌석배치도 업로드 | 1일 |

**총 예상 개발 기간: 약 10~14일**

---

## 9. 체크리스트 (개발 완료 시 확인할 항목)

### 사용자 페이지
- [ ] 메인 페이지에서 영상이 인라인으로 정상 재생됨
- [ ] 모바일 / PC 양쪽에서 레이아웃이 깨지지 않음
- [ ] 자리 찾기에서 이름 + 전화번호 뒷자리로 정확히 조회됨
- [ ] 검색 실패 시 적절한 안내 메시지가 노출됨
- [ ] 좌석배치도 이미지가 자리 찾기 페이지 하단에 항상 표시됨
- [ ] 브로셔 페이지에서 이미지 8장이 순서대로 정상 표시됨
- [ ] 브로셔 이미지가 모바일에서 확대 보기 가능함
- [ ] 브로셔 이미지 Lazy Loading 동작 (스크롤 시 순차 로드)
- [ ] 홈에 최근 응원 메시지 3건이 미리보기로 노출됨
- [ ] `/messages` 에서 응원 메시지 작성·즉시 공개가 정상 동작함
- [ ] 1분 내 2번째 작성 시 Rate Limit 안내가 표시됨

### 관리자 페이지
- [ ] `/admin` 접속 시 별도 로그인 없이 바로 페이지 표시
- [ ] CSV 업로드 시 미리보기가 표시됨
- [ ] 잘못된 포맷 CSV 업로드 시 오류가 안내됨
- [ ] 기존 명단을 새 CSV로 덮어쓰기가 정상 동작함
- [ ] 좌석배치도 이미지 교체가 정상 동작함
- [ ] 브로셔 이미지 8장 일괄 업로드가 정상 동작함
- [ ] 브로셔 이미지 개별 교체가 정상 동작함
- [ ] 업로드 직전 데이터가 자동 백업됨
- [ ] `/admin/messages` 에서 부적절 메시지 삭제 시 홈 미리보기·`/messages` 양쪽에 즉시 반영됨

### 성능 / 보안
- [ ] 페이지 로딩이 3초 이내
- [ ] HTTPS로 접속됨
- [ ] CSV / 이미지 파일 크기 제한이 적용됨
- [ ] 검색 Rate Limit이 적용됨

---

## 10. 부록

### 10.1 참고 자료
- 초대 영상: https://www.youtube.com/watch?v=0aT4IdHXZW8
- 공식 브로셔: 별첨 JPG 8장 (행사 운영진 제공)
- 좌석배치도: 행사 운영진 제공 예정

### 10.2 변경 이력

| 버전 | 일자 | 변경 내용 | 작성자 |
|---|---|---|---|
| v1.0 | 2026-05-09 | 최초 작성 | — |
| v1.1 | 2026-05-09 | 관리자 로그인 제거(`/admin` 직접 접근), 브로셔 JPG 8장 업로드 기능 추가 | — |
| v1.2 | 2026-05-21 | **응원 메시지** 기능 정식 등재 — 홈 미리보기, `/messages` 페이지 (작성 + 목록), `/admin/messages` 모더레이션, `messages` 테이블 (ENT-04), 1분/IP Rate Limit. `/messages` 에 3탭 네비게이션 도입 (URL `?tab=cheer\|review\|photos` 영속화, 응원 메시지 활성, 관람 후기·사진은 placeholder). 홈 미리보기는 같은 3개 항목을 칩 링크로 노출 (`/messages?tab=...`). 좋아요·댓글·답글, 후기·사진 콘텐츠 기능은 Out of Scope 유지 | — |

---

*문서 끝.*
